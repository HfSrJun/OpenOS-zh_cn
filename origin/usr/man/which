名称
  which - 定位命令位置

用法
  which COMMAND

描述
  该程序会将给定的各个程序的完整路径输出到标准输出。如果程序是别名则会注明。如果找不到程序，则会输出错误信息。

样例
  which ls
    显示`/bin/ls.lua`。

  which doesntexist dir
    显示`doesntexist: file not found`以及`dir: aliased to ls`。