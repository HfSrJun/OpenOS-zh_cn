名称
  wget - 通过http下载文件

用法
  wget URL [FILE]

描述
  `wget`程序可以从互联网上下载程序，需给出下载来源URL。

  `wget`程序需要用到因特网卡，还需要在模组的配置文件内启用访问互联网的功能。

选项
  -f
    覆盖文件前不进行提示
  -q
    只输出报错，不输出状态信息

样例
  wget http://example.com/data.zip
    下载文件`data.zip`并将其保存为`data.zip`。

  wget http://example.com/data.zip blah.zip
    下载文件`data.zip`并将其保存为`blah.zip`。