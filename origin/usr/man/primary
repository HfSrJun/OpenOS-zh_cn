名称
  primary - 获取或设定首选组件

用法
  primary TYPE
  primary TYPE ADDRESS

描述
  该程序可读取指定类型组件的首选组件的地址。若提供新首选组件的地址（可以为缩写），程序还能修改指定类型组件的首选组件。

样例
  primary gpu
    输出当前首选GPU的地址到标准输出。

  primary gpu 24a
    令地址以`24a`开头的GPU成为新的首选GPU。