名称
  hostname - 显示与修改主机名

用法
  hostname [NEW NAME] [--update]

样例
  hostname
    输出现在设定的主机名

  hostname test
    将这台电脑的主机名设为“test”

  hostname --update
    通过读取`/etc/hostname`来更新$HOSTNAME，以备手动设定主机名。不会在标准输出上显示。
