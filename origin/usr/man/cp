名称
  cp - 复制文件

用法
  cp SOURCE DEST
  cp SOURCE DIRECTORY

描述
  `cp`可以将一个文件在文件系统内或跨文件系统进行复制。

样例
  cp a b
    将文件`a`复制一份，在同一个目录中保存为文件`b`。

  cp /home/a.txt /home/d/
    将文件`/home/a.txt`复制一份，保存为`/home/d/a.txt`。