名称
  edit - 简易文件编辑器

用法
  edit FILE

描述
  非常简单的文本文件编辑器。要使用`edit`创建新文件，只需在可写入的文件系统中打开一个不存在的文件，并保存。

选项
  -r
    以只读模式打开文件

样例
  edit /tmp/test.txt
    打开文件`/tmp/test.txt`进行编辑。若该文件不存在，则保存时会进行创建。

  edit /bin/ls.lua
    打开文件`/bin/ls.lua`，且会在只读模式下打开。假设这里的`/bin`保持默认，是由内存提供的。